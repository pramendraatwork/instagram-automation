name: Deploy n8n

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to server
        run: |
          echo "Deployment step - will connect to AWS EC2 later"
          echo "Docker image: n8nio/n8n"
          echo "Deployment successful!"
